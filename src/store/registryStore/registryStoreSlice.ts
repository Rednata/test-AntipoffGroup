import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  token: '',
  error: '',
};

export const registryStoreSlice = createSlice({
  name: 'token',
  initialState,
  reducers: {

  }

});


